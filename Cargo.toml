[package]
name = "vector3d"
version = "0.1.1"
authors = ["David Roundy <roundyd@physics.oregonstate.edu>"]

[dependencies]
serde = "1.0"
serde_derive = "1.0"
clapme = "0.1.2"

[features]
# Treat warnings as a build error.
strict = []
