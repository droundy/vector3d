[package]
name = "vector3d"
version = "0.1.0"
authors = ["droundy <roundyd@physics.oregonstate.edu>"]

[dependencies]
serde = "1.0"
serde_derive = "1.0"

[features]
# Treat warnings as a build error.
strict = []
